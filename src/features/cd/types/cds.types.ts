export type CDDataType = {
  code: string;
  title: string;
  content: string[];
  images: string[];
  steam?: string;
  gog?: string;
  wish?: string;
  website?: string;
};

export type basicInfoCDType = {
  code: string;
  preview: string;
};
